<li class="dropdown">
    <a class="dropdown-toggle" href="#" data-toggle="dropdown">
        <span class="glyphicon glyphicon-th"></span>
            {{ 'layout.choose_application'|trans({}, 'messages') }}
        <span class="caret"></span>
    </a>
    <ul class="dropdown-menu pull-right" role="menu">
        {% for application in applications %}
        <li class="">
            <a class="btn" href="{{ path('canal_tp_sam_ecore_application_manager_choose_application', {application : application.id}) }}">
                {{ application.name }}
            </a>
        </li>
        {% endfor %}
    </ul>
</li>
